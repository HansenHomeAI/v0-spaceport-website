reason: ladder_2000 job md1p2ea692k-1776292719 on ea69a487 cleared through merger_08, then failed at component bridge recovery for merger_09. The runtime reused pending seed models whose binary/text artifacts had already been deleted by run_parent_seam_registration() when reindex_model_to_database() returned the original seed model, so image_registrator hit non-directory input paths for both chunk_12_13_merge_bridge_seed_01_point_triangulator_02 and chunk_12/sparse_initial/0.
last_step: I created child branch/worktree agent-30377461-sfm-seed-artifact-preserve from ea69a487, patched run_parent_seam_registration() so cleanup preserves the original seed model when superseded cycle models or the pre-BA current model are retired, added a regression that exercises the direct seed-reuse path, and validated with python3 -m unittest tests.unit.test_colmap_gps_priors tests.unit.test_prior_chunk_benchmark_subset (96 tests, OK).
next_unblocked_step: commit and push the seed-artifact preservation fix, watch Trigger ML Container Build / CDK Deploy / Pages to green, resolve the fresh branch digest, and rerun ladder_2000 on the repaired image. If ladder_2000 passes, promote to full MD1 on the same digest; if it fails, use the new sfm_metadata.json failure fields and CloudWatch logs to patch the next narrow boundary.
owner_action_needed: none
updated: 2026-04-15T22:27:30Z
