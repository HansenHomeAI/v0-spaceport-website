reason: the latest full MD1 proof run md1p2dd70full-1776233149 cleared startup and the earlier disk-pressure boundary, but it still failed at hierarchical merge connectivity: chunk_model_merger_07 could not produce a usable merge across 14 pending components. A local patch now adds component-aware bridge/seam recovery inside merge_chunk_models(), reinserts usable connector models into the pending merge pool, and writes compact merge diagnostics into sfm_metadata.json.
last_step: I implemented the merge-recovery patch on dd70eb0b in run_colmap_sfm.py, added focused regressions for connector reinsertion, full-component seam scope, and merge-failure summaries in tests.unit.test_colmap_gps_priors, and validated the change with python3 -m unittest tests.unit.test_colmap_gps_priors tests.unit.test_prior_chunk_benchmark_subset (94 tests, OK). The next cloud step is to push this patch, wait for Trigger ML Container Build / CDK Deploy / Pages to go green, then rerun the cheaper phase-2 hierarchy gate before another full-MD1 spend.
next_unblocked_step: push the merge-recovery patch, watch the branch workflows to green, resolve the fresh branch digest, then rerun ladder_2000 on the repaired image. If ladder_2000 passes, rerun full MD1; if it fails, use the new chunk_merge_summary / merge_component_recovery_records diagnostics to patch the next concrete merge boundary.
owner_action_needed: none
updated: 2026-04-15T10:23:15Z
