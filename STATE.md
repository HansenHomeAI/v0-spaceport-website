reason: validating whether monolithic `global_mapper` can beat the ladder_2000 chunked baseline once the shipped SfM image has real GPU sparse bundle adjustment and GPU global positioning enabled
last_step: pushed `ce9cf8d5`, watched Pages go green and CDK go green again, then saw Trigger ML Container Build fail in CodeBuild 616 because Ceres rejected `cudss_DIR=/usr/lib/x86_64-linux-gnu/libcudss/12/cmake/cudss` with `Header directory containing file cudss.h was not found relative to /usr/lib/x86_64-linux-gnu/libcudss/12/cmake/cudss!`; patched `infrastructure/containers/sfm/Dockerfile.base` again to restore the canonical `/usr/lib/x86_64-linux-gnu/cmake/cudss` symlink layout plus top-level cuDSS header symlinks before configuring Ceres, and then pointed `cudss_DIR` at that canonical path instead of the deeper real directory
next_unblocked_step: bump `web/trigger-dev-build.txt`, commit and push the cuDSS package-layout fix, watch Pages/CDK/Trigger ML Container Build to green, rerun the cheap capability snapshot against the rebuilt image, and only then spend on another real-graph benchmark
owner_action_needed: none
updated: 2026-04-18T02:10:18Z
