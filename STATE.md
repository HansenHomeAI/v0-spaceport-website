reason: validating whether monolithic `global_mapper` can beat the ladder_2000 chunked baseline once the shipped SfM image has real GPU sparse bundle adjustment and GPU global positioning enabled
last_step: watched Trigger ML Container Build fail again in CodeBuild 623 after the full Ceres and COLMAP compile/install succeeded; the verifier showed `colmap help` failed at runtime with `libcudss.so.0: cannot open shared object file`, so the remaining blocker is the loader path for Ubuntu's cuDSS shared library directory, not GPU support itself. Patched `infrastructure/containers/sfm/Dockerfile.base` to register `/usr/lib/x86_64-linux-gnu/libcudss/12` with `ldconfig` and to assert that `ldd /usr/local/bin/colmap` resolves `libcudss.so.0` before accepting the image
next_unblocked_step: bump `web/trigger-dev-build.txt`, commit and push the cuDSS runtime-loader fix, watch Pages/CDK/Trigger ML Container Build for the new SHA, confirm the image actually publishes, rerun the cheap capability snapshot only if the image build succeeds, and only then spend on another real-graph benchmark
owner_action_needed: none
updated: 2026-04-18T06:04:57Z
