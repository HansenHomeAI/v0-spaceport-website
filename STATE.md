ACTIVE: CV-HR SfM-authority 3DGS R19 isolation
reason: R6 completed leaf training, strict-core merge, and frozen-camera review, but the hard visual QA gate blocked promotion on horizon foreground saturation. R7 proved the original-name priority-camera alias bug. R8 proved the alias fix and merge contract, but hard visual QA still blocked on hazy/blue foreground splats, absolute quality thresholds, and horizon non-regression. R9 and R10 progressively pruned more sky-like gaussians, but the merged renders still failed hard visual QA. R11 full-res was diagnostic-only because it omitted scaffold reuse. R12 restored scaffold reuse and only changed `NEVER_MASK_UPPER=0.0`, but still blocked on horizon foreground saturation and horizon V18 non-regression. R13 post-export pruning almost cleared alpha but regressed PSNR/LPIPS. R14 disabled robust mask and worsened horizon alpha (`DJI_00801.JPG` alpha_mean `0.9909336566925049`). Work must continue only from `/Users/gabrielhansen/worktrees/agent-52689431-3dgs-sfm-authority`; never edit the root checkout `/Users/gabrielhansen/Documents/v0-spaceport-website`, which must remain clean on `development` for user/other-agent work.
last_step: Pages retry `26625256660` is green with preview `https://agent-52689431-3dgs-sfm-auth.v0-spaceport-website-preview2.pages.dev`; Playwright MCP waitlist flow passed after updating `scripts/run_waitlist_flow.mjs` for current MCP snapshot output. R19 tile_00-only merge completed and preflight passed, so frozen-camera review job `cvhr-sfm-auth-r19-tile00only-1780042500-review` is now submitted. R19 sky-color-wide export/prune has materialized a passing preflight artifact with 189,516 remaining splats and `sky_color_removed_gaussians=12056`, but SageMaker still reports the job `InProgress`.
next_unblocked_step: wait for `cvhr-sfm-auth-r19-tile00only-1780042500-review` and official completion of `cvhr-sfm-auth-r19-skycolor-wide-1780041120-tile-00`; then run hard visual QA for tile_00-only review and only merge/review sky-color-wide after official leaf completion
owner_action_needed: none currently
automations: tmux `codex-3dgs-health` logs branch/AWS/GitHub/state health every 5 minutes to `logs/automation/codex-3dgs-health.log`; `codex-3dgs-build-watch` logs GitHub/CodeBuild health every 2 minutes; `codex-3dgs-r19-tile00only-review-watch` polls the tile_00-only review job; `codex-3dgs-r19-wide-watch` and `codex-3dgs-r19-skycolor-watch` poll the sky-color-wide export/prune job; `codex-3dgs-autonomy` is armed as a delayed `scripts/codex-loop.sh` backstop from this worktree only
updated: 2026-05-29T08:03:00Z
