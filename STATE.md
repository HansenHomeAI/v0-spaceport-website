reason: the live full MD1 run md1p27ebafull-1776125456 on digest sha256:98ae2cc3ae53ceec4e82b5717a3d4dcc3db659bab6146730d935bf4c6d0508cc wedged after chunk_15 succeeded. CloudWatch shows chunk_15_mapper_initial finished 176/176 images and then the pipeline threw inside prepare_chunk_database() during the next chunk-db prune step. I patched the SQLite chunk-clone path to normalize the global source DB before chunking and rebuild a corrupted chunk clone once before failing, and I stopped the wedged SageMaker job to save compute.
last_step: I reproduced the failure boundary from the live full run, narrowed it to prepare_chunk_database() after a successful chunk_15 leaf reconstruction, updated infrastructure/containers/sfm/run_colmap_sfm.py to normalize the global source DB for chunking and retry a corruption-class DatabaseError by rebuilding the chunk clone from a re-normalized source, added focused regressions in tests/unit/test_colmap_gps_priors.py, and reran python3 -m unittest tests.unit.test_colmap_gps_priors tests.unit.test_prior_chunk_benchmark_subset locally (85 tests passed). The stopped full-run job is md1p27ebafull-1776125456.
next_unblocked_step: commit and push the SQLite chunk-db hardening patch, watch the new branch workflows and container build to green, resolve the fresh branch image digest, and relaunch the full MD1 run on that digest. After the relaunch, monitor to terminal state and harvest the final sfm_metadata.json proof.
owner_action_needed: none
updated: 2026-04-14T06:10:00Z
