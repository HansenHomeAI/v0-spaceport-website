ACTIVE: CV-HR SfM-authority 3DGS R28 horizon-coverage pruning canary
reason: Work continues only from `/Users/gabrielhansen/worktrees/agent-52689431-3dgs-sfm-authority`; never edit the root checkout `/Users/gabrielhansen/Documents/v0-spaceport-website`, which must remain clean on `development` for user/other-agent work. R27 proved direct-path scale pruning is wired and mechanically safe, but the frozen-camera review still blocks production quality on horizon foreground coverage.
last_step: R27 review job `cvhr-sfm-auth-r27-mixed-1780044317-review` officially completed. Hard visual QA remains `blocked`: V18 non-regression promotes, strict-core merge/preflight passed, but `DJI_00809.JPG` still has horizon foreground saturation (`alpha_coverage_gt_005=0.9993453333333333`, `alpha_mean=0.9505199790000916`) and absolute metrics remain below production thresholds. Implemented opt-in priority-horizon coverage pruning under `FLOATER_PRUNING_HORIZON_COVERAGE_*`, plus `FLOATER_PRUNING_INCLUDE_PRIORITY_CONTEXT` so the no-retrain export pass can include frozen review cameras like `DJI_00809.JPG` for projection analysis even when they are outside tile_07's training selection. Focused py_compile and unittest checks passed locally without Docker.
next_unblocked_step: commit/push the R28 pruning code and small R27 proof logs from this worktree, watch CDK/Pages/ML Container Build to green, capture the exact 3DGS image digest, then launch a no-training R28 tile_07 ProcessingJob from the corrected R21 artifact with R24/R27 pruning plus priority-context and horizon-coverage flags before cached merge/review.
owner_action_needed: none currently
automations: passive tmux watchers `codex-3dgs-health`, `codex-3dgs-build-watch`, `codex-3dgs-r27-tile07-watch`, `codex-3dgs-r27-mixed-merge-watch`, and `codex-3dgs-r27-mixed-review-watch` are active; app-native `automation_update` is not available in this session, so accountability is being handled by pushed CI/CodeBuild watchers plus tmux monitors from the worktree.
updated: 2026-05-29T13:25:02Z
