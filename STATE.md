ACTIVE: CV-HR SfM-authority 3DGS R19 isolation
reason: R6 completed leaf training, strict-core merge, and frozen-camera review, but the hard visual QA gate blocked promotion on horizon foreground saturation. R7 proved the original-name priority-camera alias bug. R8 proved the alias fix and merge contract, but hard visual QA still blocked on hazy/blue foreground splats, absolute quality thresholds, and horizon non-regression. R9 and R10 progressively pruned more sky-like gaussians, but the merged renders still failed hard visual QA. R11 full-res was diagnostic-only because it omitted scaffold reuse. R12 restored scaffold reuse and only changed `NEVER_MASK_UPPER=0.0`, but still blocked on horizon foreground saturation and horizon V18 non-regression. R13 post-export pruning almost cleared alpha but regressed PSNR/LPIPS. R14 disabled robust mask and worsened horizon alpha (`DJI_00801.JPG` alpha_mean `0.9909336566925049`). Work must continue only from `/Users/gabrielhansen/worktrees/agent-52689431-3dgs-sfm-authority`; never edit the root checkout `/Users/gabrielhansen/Documents/v0-spaceport-website`, which must remain clean on `development` for user/other-agent work.
last_step: Pages run `26626009310` is green for commit `68c35989`, with preview `https://agent-52689431-3dgs-sfm-auth.v0-spaceport-website-preview2.pages.dev` and hash URL `https://c60365e0.v0-spaceport-website-preview2.pages.dev`; Playwright MCP waitlist flow passed against that URL. R19 tile_00-only review completed and hard visual QA blocked on quality/non-regression, but it passed `horizon_foreground_saturation`, proving tile_07 contributes the opaque sky-like foreground. Implemented the next smallest code fix: bounded leaf selection now preserves explicit frozen boundary/horizon cameras before generic bucket sampling, so tile_07 can retain `DJI_00801.JPG` when it is context under the cap.
next_unblocked_step: commit/push the frozen-camera selection fix, monitor CDK/ML container rebuild to green, resolve the rebuilt 3DGS image digest, then run a bounded tile_07 canary with explicit `HORIZON_FROZEN_CAMERAS=DJI_00801.JPG,DJI_00809.JPG` before another merge/review
owner_action_needed: none currently
automations: tmux `codex-3dgs-health` logs branch/AWS/GitHub/state health every 5 minutes to `logs/automation/codex-3dgs-health.log`; `codex-3dgs-build-watch` logs GitHub/CodeBuild health every 2 minutes; `codex-3dgs-autonomy` is armed as a delayed `scripts/codex-loop.sh` backstop from this worktree only
updated: 2026-05-29T08:17:22Z
