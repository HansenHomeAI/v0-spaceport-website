ACTIVE: CV-HR SfM-authority 3DGS cache-contract rebuild before R10
reason: R6 completed leaf training, strict-core merge, and frozen-camera review, but the hard visual QA gate blocked promotion on horizon foreground saturation: `DJI_00801.JPG` alpha_mean 0.9942 / coverage 1.0 and `DJI_00809.JPG` alpha_mean 0.9914 / coverage 1.0. R7 proved the original-name priority-camera alias bug. Commit `c18fd9b7ad7f98f6042d2bcb06815d0185d60ff8` patched pruning alias matching; CDK run `26593241873`, ML workflow `26593241977`, and CodeBuild `spaceport-ml-containers:4b2e0b8a-8714-486f-a783-3f466a32d446` are green. R8 proved the alias fix and merge contract, but hard visual QA still blocks promotion on hazy/blue foreground splats, absolute quality thresholds, and horizon non-regression. R9 tile_00 passed leaf preflight after pruning 48,954 sky-like gaussians versus 7,498 in R8 and keeping `splat_vertex_count=300884` inside the reference guard. Work must continue only from `/Users/gabrielhansen/worktrees/agent-52689431-3dgs-sfm-authority`; the root checkout `/Users/gabrielhansen/Documents/v0-spaceport-website` is clean on `development` for user/other-agent work.
last_step: R9 review completed and hard visual QA blocked. Merge contract stayed clean, but production visual gates failed on horizon foreground saturation, absolute PSNR/SSIM/LPIPS, and horizon non-regression. Manual panel inspection confirmed broad white/blue haze and blur. No promotion/compression. A cache-safety bug was found and patched: tile input hashes now include effective default training env, downscale, pruning, background/export, and density-cap settings, so R8/R9-style pruning changes cannot share stale cache hashes. Focused unit tests pass.
next_unblocked_step: commit/push the R9 evidence plus cache-hash fix, wait for CDK/ML/CodeBuild, then run a no-spend R10 dry-run with the rebuilt image before any additional one-tile canary spend
owner_action_needed: none currently
automations: tmux `codex-3dgs-health` logs branch/AWS/GitHub/state health every 5 minutes to `logs/automation/codex-3dgs-health.log`; `codex-3dgs-build-watch` logs GitHub/CodeBuild health every 2 minutes; `codex-3dgs-r8-watch` now tracks R9 tile_00 status every 3 minutes to `logs/automation/cvhr-r9-tile00-watch.log`; `codex-3dgs-autonomy` is armed as a one-hour foreground-stall backstop via `scripts/codex-loop.sh`
updated: 2026-05-28T21:27:00Z
