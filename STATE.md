reason: the live 1000-image ladder rung md1p2ea411k-1776104833 on digest sha256:cafa2a125dd7aa9691d5b7a4c80092be73925f91bd7059c90d94900188aab02f is still in progress, but I found and fixed a real planner cost bug where leftover unassigned images could bypass the configured leaf hard cap by being appended as a single spillover chunk
last_step: while md1p2ea411k-1776104833 advanced cleanly into feature extraction, I audited the footprint_graph_v1 planner and found that build_footprint_graph_chunks appended remaining_names directly when the tail length was >= chunk_min_images, which could create oversized spillover core chunks and waste mapper time. I updated infrastructure/containers/sfm/run_colmap_sfm.py to split spillover leftovers into capped chunks and also fixed representative_chunk_names so bridge-anchor selection handles set inputs safely. I extended tests/unit/test_colmap_gps_priors.py with a targeted spillover-cap regression and reran python3 -m unittest tests.unit.test_colmap_gps_priors tests.unit.test_prior_chunk_benchmark_subset locally; all 83 tests passed.
next_unblocked_step: commit and push the spillover-cap patch, watch the new GitHub runs and container build to green, then arm a new commit-specific promotion watcher. Let the current ea41 ladder keep running until the new digest is available; if ea41 fails before then, relaunch 1000 on the new digest and continue automatic promotion through 2000 and full.
owner_action_needed: none
updated: 2026-04-13T18:50:00Z
