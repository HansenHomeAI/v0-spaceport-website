reason: validating whether monolithic `global_mapper` can beat the ladder_2000 chunked baseline once the shipped SfM image has real GPU sparse bundle adjustment and GPU global positioning enabled
last_step: ran the exact `probe_subsets.geometry_mix` benchmark as `glomapmix-1776476480`, observed `COLMAP[mapper_global_spatial_sequential_only] ... Requested to use GPU for bundle adjustment, but Ceres was compiled without cuDSS support. Falling back to CPU-based sparse solvers.` during `global_positioning`, stopped the job to save compute, traced the problem back to CodeBuild 615 reporting `Could NOT find cudss (missing: cudss_DIR)` while the handwritten build-info marker still claimed cuDSS support, and patched `infrastructure/containers/sfm/Dockerfile.base` to pass the detected `cudss_DIR` into the Ceres configure step and fail the image build if installed Ceres still defines `CERES_NO_CUDSS`
next_unblocked_step: bump `web/trigger-dev-build.txt`, commit and push the cuDSS wiring fix, watch Pages/CDK/Trigger ML Container Build to green, rerun the cheap capability snapshot against the rebuilt image, and only then spend on another real-graph benchmark
owner_action_needed: none
updated: 2026-04-18T02:05:34Z
