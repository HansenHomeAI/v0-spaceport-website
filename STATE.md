ACTIVE: CV-HR SfM-authority 3DGS R27 scale-outlier canary rebuild
reason: Work continues only from `/Users/gabrielhansen/worktrees/agent-52689431-3dgs-sfm-authority`; never edit the root checkout `/Users/gabrielhansen/Documents/v0-spaceport-website`, which must remain clean on `development` for user/other-agent work. R19 tile00-only review proved old tile07 was the horizon foreground polluter. R21 retrained tile07 with the fixed explicit frozen-camera selection path, preserved/weighted `DJI_00801.JPG`, passed leaf preflight/gate, and officially completed in SageMaker.
last_step: R26 exposed a wiring gap before merge/review spend: the direct no-training PLY path logged `scale_pruning: None` because `run_export_quality_pass.py` called floater pruning and density cap but skipped `prune_exported_foreground_scale_outliers()`. The useless R26 processing job was stopped while still in control-plane/upload tail. Patched the direct PLY path to run scale pruning between floater pruning and density cap, and targeted tests passed: `python3 -m unittest tests.unit.test_run_export_quality_pass tests.unit.test_sky_quality tests.unit.test_tiled_nerfstudio_dispatcher` plus py_compile.
next_unblocked_step: commit/push the direct-path scale-pruning fix, watch CDK/Pages/ML build to green, resolve the new exact image digest, then submit R27 no-training tile_07 scale-prune canary from the corrected R21 artifact.
owner_action_needed: none currently
automations: passive tmux watchers `codex-3dgs-health` and `codex-3dgs-build-watch` are active; the active `codex-loop.sh` backstop remains disabled because a competing loop previously issued a local `StopTrainingJob` against R20
updated: 2026-05-29T12:25:03Z
