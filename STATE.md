ACTIVE: CV-HR SfM-authority 3DGS R1 review is blocked by nonselective tile ownership, not by AWS/tool setup
reason: AWS CLI access is verified for staging `spaceport-dev` account `975050048887` and production `spaceport-prod` account `356638455876`. Branch image digest `sha256:f8d3ded5e93c22cee52bf74552739820e81bf9c9c3c70a568a23303d35542de1` rebuilt from `ffcedf38` and reran the frozen-camera R1 review, but promotion remained blocked: the foreground renders are still mostly blank/gray and one horizon view is fully blank. No-spend artifact inspection found the real failure mode: the R1 `tile_00`/`tile_07` manifests used huge `observed_points` bounds, both leaf scaffold filters kept the entire global scaffold, and strict-core merge retained 148,276/148,276 gaussians. That made the old preflight a false positive even though visual QA correctly failed.
last_step: patched the 3DGS container to report scaffold filter retention/rejection ratios and reject multi-tile SfM-authority leaf training when a tile keeps more than `GLOBAL_SCAFFOLD_MAX_FILTER_RETENTION_RATIO` of the global scaffold; focused unit slice passed 74 tests with `python3 -m unittest tests.unit.test_tiled_quality_review tests.unit.test_tiled_nerfstudio_dispatcher tests.unit.test_tiled_3dgs_benchmark_launcher tests.unit.test_gaussian_tile_pipeline`
next_unblocked_step: commit and push the no-spend guard plus review evidence, monitor CDK Deploy and ML container CodeBuild to green, then run a dry/no-spend selected-input audit before spending on any new CV-HR leaf training
owner_action_needed: none
updated: 2026-05-27T01:18:00Z
