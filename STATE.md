reason: validating whether monolithic `global_mapper` can beat the ladder_2000 chunked baseline once the shipped SfM image has real GPU sparse bundle adjustment and GPU global positioning enabled
last_step: pushed `c7cc0ad2`, watched Trigger ML Container Build fail in CodeBuild 621 only after COLMAP again fully compiled and installed, then hit our own verifier because plain-text greps still matched `CERES_NO_SPARSE` inside config.h's internal `#if defined(...)` consistency block even though the macro was not actually defined; patched `infrastructure/containers/sfm/Dockerfile.base` so the verifier now compiles a tiny translation unit that includes `ceres/internal/config.h` and fails only if the preprocessor sees `CERES_NO_CUDA`, `CERES_NO_CUDSS`, or `CERES_NO_SPARSE` as real definitions
next_unblocked_step: bump `web/trigger-dev-build.txt`, commit and push the preprocessor-based verifier fix, watch Pages/CDK/Trigger ML Container Build for the new SHA, confirm the image actually publishes, rerun the cheap capability snapshot only if the image build succeeds, and only then spend on another real-graph benchmark
owner_action_needed: none
updated: 2026-04-18T05:34:46Z
