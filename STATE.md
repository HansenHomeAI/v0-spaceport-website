ACTIVE: CV-HR SfM-authority 3DGS proof is iterating on the first bounded AWS canary
reason: Local AWS CLI access is configured and verified for `spaceport-dev` account `975050048887` and `spaceport-prod` account `356638455876`. The branch image `975050048887.dkr.ecr.us-west-2.amazonaws.com/spaceport/3dgs:agent526894313dgssfmauthority` exists and was used by canary job `cvhr-sfm-auth-r0-canary-1779834023-scaffold`. That first canary failed cheaply after 296 billable seconds because the final CV-HR SfM reducer artifact contains sparse COLMAP authority but no `images/` directory for the training channel. The 3DGS container now validates tile-selection runs against the selected scaffold/tile image set instead of requiring all 1,710 CV-HR images, so the next canary can use a staged partial image root.
last_step: submitted one-tile CV-HR canary, pulled SageMaker/CloudWatch failure evidence, patched `infrastructure/containers/3dgs/train_nerfstudio_production.py`, added `test_validate_input_data_allows_tile_selected_partial_image_root`, and passed targeted tests with the local Python 3.11 toolchain
next_unblocked_step: commit/push the selected-image validation fix to trigger the remote 3DGS container rebuild, stage a partial CV-HR training COLMAP root with final SfM sparse metadata plus the union of scaffold and tile_00 images, then rerun the bounded canary against the fresh image
owner_action_needed: none
updated: 2026-05-26T22:34:00Z
