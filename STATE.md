reason: ladder_2000 on the first repaired child branch digest sha256:abc47512a75d7fe32e7a3dde452a878161903eb41759367b85c48b38ede3f1d7 cleared the earlier merge_11 seed-path bug but then failed later at component_bridge_09. The failed rung md1p2ea692k-1776292719 reached chunk_model_merger_08 and seam_07 successfully, then component bridge recovery 09 retried chunk_12 as a seed and image_registrator faulted with `input_path is not a directory` on /tmp/.../chunk_12/sparse_initial/0. The root cause is a seam-registration cleanup gap: when reindex_model_to_database short-circuits and returns the original seed model, run_parent_seam_registration can clean that original seed directory as a superseded cycle model, leaving later bridge retries with a stale seed path.
last_step: I preserved the earlier repaired checkpoint by branching from ea69a487 onto agent-27888319-sfm-seed-preserve-retry, patched run_parent_seam_registration() so cleanup preserves the original seed model artifact paths across seam cycles and post-BA replacement, added a regression that fails when a short-circuit reindex deletes the original seed artifacts, and validated with python3 -m unittest tests.unit.test_colmap_gps_priors tests.unit.test_prior_chunk_benchmark_subset (96 tests, OK). I also stopped the duplicate rerun md1p2ea69l2k-1776348735 after discovering the earlier repaired rung had already failed and emitted usable metadata/logs.
next_unblocked_step: commit and push agent-27888319-sfm-seed-preserve-retry, watch Pages / CDK Deploy / Trigger ML Container Build to green, resolve the fresh branch digest, and rerun ladder_2000 on that new image. If ladder_2000 passes, promote to full MD1 on the same digest. If it fails, pull sfm_metadata.json and CloudWatch logs and patch only the next narrow seam/merge boundary on another fresh follow-up branch.
owner_action_needed: none
updated: 2026-04-16T08:17:30Z
