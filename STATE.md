ACTIVE: CV-HR SfM-authority 3DGS R6 horizon-pruning canary is running on SageMaker
reason: R5 SH color decode fix is remotely proved as a partial fix, but the latest R5 review-only replay still fails hard visual QA. No-spend diagnosis identified an explicit horizon foreground saturation failure: horizon review views have near-opaque foreground alpha over sky-like pixels (`DJI_00801.JPG` alpha_mean 0.9972, coverage_gt_005 1.0; `DJI_00809.JPG` alpha_mean 0.9827, coverage_gt_005 0.9939). Commit `fd12a7bfb258a92b3b08fc8128f66c5d81354867` adds this as a first-class promotion blocker in both the container quality review manifest and the standalone visual QA gate, plus floater-pruning diagnostics so the next paid canary reports which pruning predicate is too loose/tight. Remote CDK run `26583800124`, ML workflow `26583800114`, and CodeBuild `spaceport-ml-containers:08dabf81-fc78-4d99-9624-7707cad69d88` are green for the current 3DGS container. AWS CLI profiles now verify successfully with static credentials: `default` and `spaceport-dev` are account `975050048887`, `spaceport-prod` is account `356638455876`, and `spaceport-migration` is `arn:aws:iam::975050048887:user/spaceport-migration-user`.
last_step: R6 leaf-only canary completed. `tile_00` completed in 1,716 billable seconds and preflight passed with 309,721 splats / 7,677 floater-pruned gaussians. `tile_07` completed in 1,652 billable seconds and preflight passed with 307,332 splats / 9,349 floater-pruned gaussians. Total leaf compute was 3,368 billable seconds on `ml.g4dn.xlarge`, about $0.69 against the $5 cap.
next_unblocked_step: run the no-training cached strict-core merge/review using `logs/3dgs-sfm-authority/cvhr_sfm_authority_r6_tile_cache_manifest.json`, then extract quality panels and run the hard visual QA gate against the R4 baseline
owner_action_needed: none currently
automations: tmux `codex-3dgs-health` is logging branch/AWS/GitHub/state health every 5 minutes to `logs/automation/codex-3dgs-health.log`; tmux `codex-3dgs-autonomy` is armed as a foreground-stall backstop via `scripts/codex-loop.sh`
updated: 2026-05-28T16:41:34Z
