reason: the live promotion loop is still waiting on the next branch container image, and a stronger bridge-scope patch is now ready to push so seam-only bridge retries use the full bridge chunk plan instead of only the two seed chunk image sets
last_step: after confirming md1p2c4201k-1776099608 had stalled for more than 20 minutes with zero CloudWatch streams and stopping it, I traced the remaining 1000-image failure risk to a deeper seam-only bridge bug: run_parent_seam_registration_with_retry was still building its candidate scope from left/right seed chunk names, so bridge-context images present in merged_chunk_plan.image_names could be dropped entirely. I updated infrastructure/containers/sfm/run_colmap_sfm.py so select/expand frontier helpers accept candidate_names, run_parent_seam_registration_with_retry accepts scope_image_names, and bridge repair passes merged_chunk_plan.image_names into that seam path. I also extended tests/unit/test_colmap_gps_priors.py so the new regressions verify required bridge targets and extra bridge-scope images both survive into seam scope, and python3 -m unittest tests.unit.test_colmap_gps_priors tests.unit.test_prior_chunk_benchmark_subset passed locally (81 tests).
next_unblocked_step: commit and push the bridge-scope seam patch, then arm a fresh watcher keyed to the new commit. Let that watcher wait for the new GitHub workflows and automatic container build to publish a fresh branch digest, then relaunch ladder_1000 immediately on that image and continue automatic promotion through ladder_2000 and the full run if the gates pass.
owner_action_needed: none
updated: 2026-04-13T17:31:30Z
