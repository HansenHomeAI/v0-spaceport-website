reason: validating whether monolithic `global_mapper` can beat the ladder_2000 chunked baseline once the shipped SfM image has real GPU sparse bundle adjustment and GPU global positioning enabled
last_step: pushed `b4b74b79`, watched Trigger ML Container Build fail in CodeBuild 619 after cuDSS was finally detected and linked, then hit a compile failure because `/usr/include/cudss.h` includes sibling headers like `cudss_threading_interface.h` that were not mirrored into `/usr/include`; patched `infrastructure/containers/sfm/Dockerfile.base` to symlink all `/usr/include/libcudss/12/cudss*.h` headers into `/usr/include` instead of only `cudss.h` and `cudss_distributed_interface.h`
next_unblocked_step: bump `web/trigger-dev-build.txt`, commit and push the broader cuDSS header mirroring fix, watch Pages/CDK/Trigger ML Container Build for the new SHA, inspect the new CodeBuild result directly, rerun the cheap capability snapshot only if the image build succeeds, and only then spend on another real-graph benchmark
owner_action_needed: none
updated: 2026-04-18T04:52:35Z
