reason: validating whether monolithic `global_mapper` can beat the ladder_2000 chunked baseline once the shipped SfM image has real GPU sparse bundle adjustment and GPU global positioning enabled
last_step: pushed `73a2ec16`, watched Trigger ML Container Build actually enter CodeBuild 618 with the ECR `:base` parent path working, then hit a new Ceres configure failure because `/usr/lib/x86_64-linux-gnu/cmake/cudss/cudss-static-targets.cmake` referenced `/usr/lib/x86_64-linux-gnu/libcudss/12/libcudss_static.a` and that file was missing; patched `infrastructure/containers/sfm/Dockerfile.base` to install `libcudss0-static-cuda-12` in addition to the runtime and dev packages
next_unblocked_step: bump `web/trigger-dev-build.txt`, commit and push the static cuDSS package fix, watch Pages/CDK/Trigger ML Container Build to green, rerun the cheap capability snapshot against the rebuilt image, and only then spend on another real-graph benchmark
owner_action_needed: none
updated: 2026-04-18T02:29:17Z
