reason: validating whether monolithic `global_mapper` can beat the ladder_2000 chunked baseline once the shipped SfM image has real GPU sparse bundle adjustment and GPU global positioning enabled
last_step: pushed `81071690`, watched Trigger ML Container Build fail in CodeBuild 620 only after COLMAP fully compiled and installed with cuDSS-enabled Ceres, then hit our own verifier because it incorrectly required `#define CERES_USE_CUDA` even though Ceres expresses enabled CUDA/cuDSS support by leaving `CERES_NO_CUDA` and `CERES_NO_CUDSS` commented out; patched `infrastructure/containers/sfm/Dockerfile.base` so the verifier only fails on uncommented `CERES_NO_CUDA`, `CERES_NO_CUDSS`, or `CERES_NO_SPARSE`
next_unblocked_step: bump `web/trigger-dev-build.txt`, commit and push the verifier fix, watch Pages/CDK/Trigger ML Container Build for the new SHA, confirm the image actually publishes, rerun the cheap capability snapshot only if the image build succeeds, and only then spend on another real-graph benchmark
owner_action_needed: none
updated: 2026-04-18T05:14:26Z
