reason: the live 1000-image ladder rung on digest sha256:9666cb012f79796070386eeb687d0a4b765b18e07e59dbd9df7fae605d83e667 is still in progress, but a stronger seam-support patch is now ready to push so parent seam registration seeds its match list with bounded track-supported seed-image pairs before image_registrator runs
last_step: after confirming the prior ladder_1000 failure was an image_registrator unordered_map::at crash inside bridge seam repair, I traced another remaining seam-only risk to the chunk database prep path: seam databases are pruned clones with matches cleared, so even a valid seed model can still reach image_registrator with too little support for already-registered seed images. I updated infrastructure/containers/sfm/run_colmap_sfm.py to derive bounded track-supported image pairs from the seed model, inject those pairs into seam match lists, and pass them through run_parent_seam_registration. I also extended tests/unit/test_colmap_gps_priors.py to cover the new helper and the seam-registration plumbing, and python3 -m unittest tests.unit.test_colmap_gps_priors tests.unit.test_prior_chunk_benchmark_subset now passes locally (82 tests).
next_unblocked_step: commit and push the seam-support patch, then arm a new watcher keyed to that commit. Let the watcher monitor the current md1p2f3aa1k-1776103280 rung, stop it once the fresh digest is available, relaunch ladder_1000 on the new image, and continue automatic promotion through ladder_2000 and the full run if the gates pass.
owner_action_needed: none
updated: 2026-04-13T18:10:00Z
