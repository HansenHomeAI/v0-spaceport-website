ACTIVE: CV-HR SfM-authority 3DGS R5 remains visually blocked; SH color decode fix is remotely proved as a partial fix
reason: AWS CLI `spaceport-dev` and `spaceport-prod` profiles both pass STS as of `2026-05-27T23:22:00Z`; the earlier blocker was stale state plus expired single-use browser auth codes, not current account access. R5 leaf-only canary retrained `tile_00` and `tile_07` with downscale `2`, 240 selected images/tile, 6000 iterations, SH degree `3`, and strict SfM-authority bounds. Both R5 leaf preflights passed with no block reasons: `tile_00` has `322505` splats and retained `24867/74138` scaffold gaussians; `tile_07` has `300026` splats and retained `23963/74138`. The strict-core no-training merge passed with `480403/622531` gaussians retained, `142128` dropped, and no fallback/retain-all tiles. No-spend diagnosis found the review loader was decoding PLY `f_rest_*` SH coefficients as basis-major RGB triplets even though gsplat/Nerfstudio export flattens them channel-major. Commit `f1939e925b114617822bd262dc01bec82de551c8` built successfully in CodeBuild `spaceport-ml-containers:30e7e5bd-575e-433b-89c0-720604a05c20`, producing image digest `sha256:b025dffb86988de895d6bd38eea997b02379408abeca0254a0cdffc531cf9290`. Review-only replay `cvhr-sfm-auth-r5-quality-1779917117-review-shfix` completed and proves the SH fix improves R5 without retraining: near_detail `13.37/0.502/0.677` -> `14.74/0.542/0.582`, boundary `11.21/0.507/0.675` -> `13.04/0.555/0.578`, horizon `9.17/0.521/0.753` -> `9.88/0.529/0.744` for PSNR/SSIM/LPIPS. Hard visual QA is still blocked by absolute thresholds and horizon regression; manual review says the magenta/green false color is fixed but renders remain smeared/washed out and horizon has blue/cyan wash.
last_step: pushed SH decode fix, verified CDK and ML container build green, ran R5 review-only replay on the new digest, evaluated gates against R4/R3, and inspected representative panels
next_unblocked_step: no-spend diagnose remaining horizon/exposure/blur failure: compare foreground vs background sky behavior, inspect horizon view camera selection/exposure, and only then decide whether a bounded R6 canary should change training settings or review/render handling
owner_action_needed: none
updated: 2026-05-27T23:51:54Z
