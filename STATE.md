reason: validating whether monolithic `global_mapper` can beat the ladder_2000 chunked baseline once the shipped SfM image has real GPU sparse bundle adjustment and GPU global positioning enabled
last_step: pushed `eccede94` with the deploy-script fix that reuses the existing ECR `:base` image as the Dockerfile.base parent, then confirmed Trigger ML Container Build skipped CodeBuild entirely and merely retagged the pre-existing branch image digest `sha256:ef5736f832d223a23b54a6cdd59cf241ec66b0d2d198130c6e67a01d09fdc129` from `2026-04-17T19:35:42-06:00`; added a real `infrastructure/containers/sfm/Dockerfile.base` comment update so the next push must route through the SfM container build path and exercise the new deploy logic
next_unblocked_step: bump `web/trigger-dev-build.txt`, commit and push the container-tree retrigger change, watch Pages/CDK/Trigger ML Container Build to green, verify the new build no longer skips or hits Docker Hub, rerun the cheap capability snapshot against the rebuilt image, and only then spend on another real-graph benchmark
owner_action_needed: none
updated: 2026-04-18T02:20:55Z
