ACTIVE: CV-HR SfM-authority 3DGS horizon-priority pruning alias fix is ready for remote rebuild
reason: R6 completed leaf training, strict-core merge, and frozen-camera review, but the hard visual QA gate blocked promotion on horizon foreground saturation: `DJI_00801.JPG` alpha_mean 0.9942 / coverage 1.0 and `DJI_00809.JPG` alpha_mean 0.9914 / coverage 1.0. R7 tile_00 completed and mechanically passed preflight, but proved the no-spend blocker: `priority_frame_name_count=2` and `priority_frame_match_count=0` because Nerfstudio frames are converted names like `frame_00157.JPG` while `HORIZON_FROZEN_CAMERAS` carries original names like `DJI_00801.JPG`. Tile_07 was stopped immediately after launch and billed 1 second. Work must continue only from `/Users/gabrielhansen/worktrees/agent-52689431-3dgs-sfm-authority`; the root checkout `/Users/gabrielhansen/Documents/v0-spaceport-website` is clean on `development` for user/other-agent work.
last_step: Patched `sky_quality.py` to load `colmap_image_name_map.json` and match priority pruning frames through converted/original/COLMAP aliases. Added `tests/unit/test_sky_quality.py` to reproduce the original-name alias case. `python3 -m py_compile infrastructure/containers/3dgs/sky_quality.py tests/unit/test_sky_quality.py tests/unit/test_tiled_nerfstudio_dispatcher.py` passed, and focused unit tests passed.
next_unblocked_step: run full focused unit suite, commit/push the alias fix, monitor CDK/ML/CodeBuild green, then rerun the bounded one-tile R8 canary and require `priority_frame_match_count>0` before any tile_07/merge spend
owner_action_needed: none currently
automations: tmux `codex-3dgs-health` logs branch/AWS/GitHub/state health every 5 minutes to `logs/automation/codex-3dgs-health.log`; `codex-3dgs-build-watch` logs GitHub/CodeBuild health every 2 minutes; `codex-3dgs-r7-watch` logs R7 SageMaker status every 3 minutes to `logs/automation/cvhr-r7-sagemaker-watch.log`; `codex-3dgs-autonomy` is armed as a one-hour foreground-stall backstop via `scripts/codex-loop.sh`
updated: 2026-05-28T18:08:54Z
