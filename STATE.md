ACTIVE: CV-HR SfM-authority 3DGS R1 ownership fix is ready for remote build validation
reason: AWS CLI access is verified for staging `spaceport-dev` account `975050048887` and production `spaceport-prod` account `356638455876`. The prior R1 `tile_00`/`tile_07` review was blocked because both leaf scaffold filters kept the entire global scaffold and strict-core merge retained 148,276/148,276 gaussians. The code now fixes the coordinate contract instead of only detecting the bad run: native tile manifests can refresh bounds from converted COLMAP support after NerfStudio conversion, the trainer uses the original training-channel sparse model for that refresh, and global scaffold export now uses planner-frame foreground PLYs so scaffold init, tile bounds, merge, and review share the same frame. The nonselective scaffold-retention guard from `69c739005315ecc9941df5340eb7faa78c8c2611` remains active.
last_step: patched planner-frame bound refresh and planner-frame scaffold export; focused 3DGS unit suite passed 76 tests
next_unblocked_step: commit and push this fix, wait for remote CDK/CodeBuild green, then run a no-spend/dry scaffold-retention audit before any new CV-HR leaf training spend
owner_action_needed: none
updated: 2026-05-27T01:48:58Z
