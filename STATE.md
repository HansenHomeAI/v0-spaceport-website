ACTIVE: CV-HR SfM-authority 3DGS R3 tile_07 coverage guard fixed before retry
reason: AWS CLI access is verified for staging `spaceport-dev` account `975050048887` and production `spaceport-prod` account `356638455876`. Commit `0a3627b61e354cf46c62eb1026663ba639a39600` fixes the coordinate contract: native tile manifests refresh bounds from converted COLMAP support after NerfStudio conversion, the trainer uses the original training-channel sparse model for that refresh, and global scaffold export uses planner-frame foreground PLYs. Remote build proof is green: CDK run `26485886987`, ML trigger run `26485886962`, CodeBuild `spaceport-ml-containers:5617f6ac-baf4-4176-bcd4-69927183a12c`, ECR `spaceport/3dgs:agent526894313dgssfmauthority` digest `sha256:7c713658049863b3785ecfa5441fbc6da04511cbcc560a41ed9d0c8f2d46cd45`. Bounded CV-HR R2 canary `cvhr-sfm-auth-r2-framefix-1779848026` completed scaffold and `tile_00`; scaffold billable time `556s`, tile billable time `712s`, total `1268s`/`0.3522h`, about `$0.259` at the tracked `ml.g4dn.xlarge` rate. The old R1 failure retained `74138/74138` scaffold gaussians; R2 retained `24896/74138`, rejected `49242`, retention ratio `0.335806`, `scaffold_filter_selective=true`, `fallback_used=false`, and exported foreground in `planner` frame. The first R3 `tile_07` retry failed fast before training because it reused the tile_00-only selected input: `326` required images, `257` available, `69` missing. A launcher input-image coverage guard is now patched and unit-tested so this mismatch blocks before SageMaker submit. No-spend proof: `cvhr_sfm_authority_r3_tile07_coverage_blocked_dry_run.json` blocks the bad input; `cvhr_sfm_authority_r3_tile07_two_tile_input_dry_run.json` passes using `s3://spaceport-ml-processing-staging/manual-validations/cvhr-sfm-auth-r1-two-tile-00-07-input-20260526/colmap` with `326/326` images available.
last_step: isolated R3 tile_07 failure to missing selected images, patched launcher no-spend coverage guard, and verified the two-tile CV-HR input passes the guard
next_unblocked_step: commit/push the coverage guard, wait for remote CDK/container build, then rerun bounded `tile_07` using the two-tile input and existing planner-frame scaffold; if leaf preflight passes, strict-core merge `tile_00` + `tile_07` and run frozen visual review
owner_action_needed: none
updated: 2026-05-27T03:06:45Z
