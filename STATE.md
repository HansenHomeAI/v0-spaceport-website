ACTIVE: CV-HR SfM-authority 3DGS R5 is visually blocked; SH color decode fix is ready for remote build/review replay
reason: AWS CLI `spaceport-dev` and `spaceport-prod` profiles both pass STS as of `2026-05-27T23:22:00Z`; the earlier blocker was stale state plus expired single-use browser auth codes, not current account access. R5 leaf-only canary retrained `tile_00` and `tile_07` with downscale `2`, 240 selected images/tile, 6000 iterations, SH degree `3`, and strict SfM-authority bounds. Both R5 leaf preflights passed with no block reasons: `tile_00` has `322505` splats and retained `24867/74138` scaffold gaussians; `tile_07` has `300026` splats and retained `23963/74138`. The strict-core no-training merge passed with `480403/622531` gaussians retained, `142128` dropped, and no fallback/retain-all tiles. Frozen review rendered nonblank panels but hard visual QA remains blocked: near_detail `13.37/0.502/0.677`, boundary `11.21/0.507/0.675`, horizon `9.17/0.521/0.753` for PSNR/SSIM/LPIPS. Manual panel review shows recognizable geometry with strong magenta/green false color, washed highlights, and smear. No-spend diagnosis found the review loader was decoding PLY `f_rest_*` SH coefficients as basis-major RGB triplets even though gsplat/Nerfstudio export flattens them channel-major; this is patched locally and covered by focused tests. The preflight script now disables argparse abbreviation so `--tile-id` cannot silently stand in for `--tile-ids`.
last_step: patched PLY SH rest decoding, added unit coverage, fixed test stub leakage, verified parser typo guard fails closed, and reran focused unit tests successfully
next_unblocked_step: commit/push the SH decode fix and evidence, monitor GitHub Actions/CodeBuild to a fresh 3DGS image digest, then run a review-only R5 replay against the existing merged artifact before any new training spend
owner_action_needed: none
updated: 2026-05-27T23:25:51Z
