ACTIVE: CV-HR SfM-authority 3DGS R18 sky-color pruning patch
reason: R6 completed leaf training, strict-core merge, and frozen-camera review, but the hard visual QA gate blocked promotion on horizon foreground saturation. R7 proved the original-name priority-camera alias bug. R8 proved the alias fix and merge contract, but hard visual QA still blocked on hazy/blue foreground splats, absolute quality thresholds, and horizon non-regression. R9 and R10 progressively pruned more sky-like gaussians, but the merged renders still failed hard visual QA. R11 full-res was diagnostic-only because it omitted scaffold reuse. R12 restored scaffold reuse and only changed `NEVER_MASK_UPPER=0.0`, but still blocked on horizon foreground saturation and horizon V18 non-regression. R13 post-export pruning almost cleared alpha but regressed PSNR/LPIPS. R14 disabled robust mask and worsened horizon alpha (`DJI_00801.JPG` alpha_mean `0.9909336566925049`). Work must continue only from `/Users/gabrielhansen/worktrees/agent-52689431-3dgs-sfm-authority`; never edit the root checkout `/Users/gabrielhansen/Documents/v0-spaceport-website`, which must remain clean on `development` for user/other-agent work.
last_step: Implemented a default-off sky-color foreground pruning path for exported splats. It removes gaussian colors that are themselves sky-like and supported by sky patches, even when the old edge-support rule would protect them. Added env wiring, config defaults, launcher cache-fingerprint keys, and tests. Local setup gap was fixed by installing user-site `pillow` and `opencv-python-headless`; focused tests pass: `tests.unit.test_sky_quality` (2 tests), `tests.unit.test_tiled_nerfstudio_dispatcher`, `tests.unit.test_tiled_3dgs_benchmark_launcher`, `tests.unit.test_run_export_quality_pass` (48 tests), and `py_compile`.
next_unblocked_step: commit/push the R18 sky-color pruning patch from the worktree, monitor CDK/ML container rebuild, then run one no-retrain export/prune canary on the existing R15 or R17 tile_00 artifact with `FLOATER_PRUNING_SKY_COLOR_ENABLED=true` before any new leaf training spend
owner_action_needed: none currently
automations: tmux `codex-3dgs-health` logs branch/AWS/GitHub/state health every 5 minutes to `logs/automation/codex-3dgs-health.log`; `codex-3dgs-build-watch` logs GitHub/CodeBuild health every 2 minutes; `codex-3dgs-r17-review-watch` polls the active review job; `codex-3dgs-autonomy` is armed as a delayed `scripts/codex-loop.sh` backstop from this worktree only
updated: 2026-05-29T07:03:35Z
