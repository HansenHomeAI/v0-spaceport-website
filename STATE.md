ACTIVE: CV-HR SfM-authority 3DGS R4 is no-spend planned; AWS re-auth is needed before any paid run
reason: Remote proof through commit `0823c2917e84d0e1defabb27b8730485d67f69dd` is green: CDK Deploy run `26489807316` succeeded after the hard visual QA gate landed. R3 was mechanically fixed but visually blocked: R2 `tile_00` and R3 `tile_07` trained, strict-core merge retained `48380/48747` gaussians with no fallback/retain-all tiles, frozen-camera review rendered nonblank panels, but visual metrics were below promotion quality (near_detail PSNR `10.836`, SSIM `0.363`, LPIPS `0.943`; boundary PSNR `9.583`, SSIM `0.386`, LPIPS `0.935`; horizon PSNR `10.928`, SSIM `0.578`, LPIPS `0.786`). R4 no-spend planning is now bounded to `tile_00` + `tile_07` on `ml.g4dn.xlarge`, 3000 iterations, downscale 4, 160 selected images/tile, strict-core merge, checkpoints every 1000 steps, frozen boundary/horizon cameras, and the CV-HR R3 review as the dataset-specific comparison baseline. Dry-run image coverage passes for both tiles at `160/160`, and estimated cost is `$2.944` under the `$5.00` cap. A local launcher patch allows a dataset-specific `--baseline-review-manifest-s3-uri` to satisfy paid-run comparison guardrails instead of incorrectly requiring an MD1 V18 manifest for CV-HR; focused tests pass. Current AWS CLI `spaceport-dev` session has expired, so SageMaker/CodeBuild validation cannot continue until `aws login --profile spaceport-dev` is completed again.
last_step: verified latest pushed GitHub Actions are green, confirmed watchdog session exists but is stale against an old CodeBuild id, reran focused tests for the dataset-baseline guard, and recorded that AWS staging credentials are expired
next_unblocked_step: commit/push the dataset-baseline guard plus R4 no-spend plan evidence, watch the new GitHub Actions run, then re-authenticate `spaceport-dev` and submit the bounded R4 two-tile quality canary only after the no-spend plan remains green
owner_action_needed: reauthenticate staging with `aws login --profile spaceport-dev` before any new AWS/SageMaker run
updated: 2026-05-27T19:01:32Z
