reason: validating whether monolithic `global_mapper` can beat the ladder_2000 chunked baseline once the shipped SfM image has real GPU sparse bundle adjustment and GPU global positioning enabled
last_step: pushed `6f36eacd`, watched CDK go green again, then saw Trigger ML Container Build fail in CodeBuild 617 before the Ceres step because Docker Hub rate-limited `nvidia/cuda:12.6.3-devel-ubuntu22.04` and the new content-addressed base tag was absent; patched `scripts/deployment/deploy.sh` so `Dockerfile.base` rebuilds reuse the existing ECR `:base` image as their parent when available instead of going back to Docker Hub for the raw CUDA base on every branch rebuild
next_unblocked_step: bump `web/trigger-dev-build.txt`, commit and push the ECR-base-parent deployment fix, watch Pages/CDK/Trigger ML Container Build to green, rerun the cheap capability snapshot against the rebuilt image, and only then spend on another real-graph benchmark
owner_action_needed: none
updated: 2026-04-18T02:17:11Z
