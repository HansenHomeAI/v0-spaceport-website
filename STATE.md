ACTIVE: CV-HR SfM-authority 3DGS R5 remains visually blocked; local no-spend hardening is continuing while AWS CLI auth is expired
reason: R5 SH color decode fix is remotely proved as a partial fix, but the latest R5 review-only replay still fails hard visual QA. No-spend diagnosis now identifies an explicit horizon foreground saturation failure: horizon review views have near-opaque foreground alpha over sky-like pixels (`DJI_00801.JPG` alpha_mean 0.9972, coverage_gt_005 1.0; `DJI_00809.JPG` alpha_mean 0.9827, coverage_gt_005 0.9939). The previous review manifest only blocked blank foreground renders, so opaque blue/cyan sky foreground could hide under generic PSNR/SSIM/LPIPS failures. Local patches now add this as a first-class promotion blocker in both the container quality review manifest and the standalone visual QA gate, and add floater-pruning diagnostics so the next paid canary reports which pruning predicate is too loose/tight. Focused unit tests and py_compile pass. AWS CLI profiles `spaceport-dev` and `spaceport-prod` both currently return `Your session has expired. Please reauthenticate using 'aws login'`, so no new SageMaker/CodeBuild/AWS artifact fetch can be launched locally until the owner reauthenticates.
last_step: added horizon foreground saturation gates, proved the current R5 shfix manifest is blocked for `horizon_foreground_saturation`, added floater-pruning predicate diagnostics, and verified `python3 -m unittest tests.unit.test_tiled_quality_review tests.unit.test_tiled_3dgs_benchmark_launcher tests.unit.test_md1_visual_qa_gate`, `python3 -m py_compile scripts/3dgs/evaluate_md1_visual_qa_gate.py infrastructure/containers/3dgs/run_tiled_quality_review.py infrastructure/containers/3dgs/sky_quality.py`, and `git diff --check`
next_unblocked_step: commit/push the no-spend hardening and evidence, monitor GitHub Actions, then continue local diagnosis of floater pruning thresholds while waiting for AWS reauth; after reauth, run only a bounded R6 review/training canary that directly tests horizon foreground alpha reduction
owner_action_needed: reauthenticate AWS CLI profiles before any paid or AWS-backed iteration: `aws login --profile spaceport-dev` and `aws login --profile spaceport-prod`
automations: tmux `codex-3dgs-health` is logging branch/AWS/GitHub/state health every 5 minutes to `logs/automation/codex-3dgs-health.log`; tmux `codex-3dgs-autonomy` is armed to resume via `scripts/codex-loop.sh` if this foreground session stalls
updated: 2026-05-28T15:12:38Z
