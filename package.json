{
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "start": "cd web && npm run start",
    "cf:build": "cd web && npm run cf:build",
    "cf:preview": "cd web && npm run cf:preview"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.18.1",
    "@playwright/test": "^1.55.1"
  }
}
