import type { ReactNode } from "react";

export default function SogsMigratedViewerLayout({ children }: { children: ReactNode }) {
  return children;
}

export const metadata = {
  title: "sogs-migrated-viewer",
  description: "Canyon-Vista UI with SOGS / SuperSplat viewer",
};
