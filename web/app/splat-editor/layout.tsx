import type { ReactNode } from "react";

export const metadata = {
  title: "splat-editor",
  description: "Local 3DGS splat editor",
};

export default function SplatEditorLayout({ children }: { children: ReactNode }) {
  return children;
}

